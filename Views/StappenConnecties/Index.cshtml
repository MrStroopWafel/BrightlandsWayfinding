@model IEnumerable<BrightlandsCasus.Models.StapConnectie>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.VanId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NaarId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Afstand)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RouteUitelg)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VanId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NaarId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Afstand)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RouteUitelg)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
